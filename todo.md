Tareas a realizar

(listo) 1_ Configurar el sistema de comandos para que muestren todas las carpeas disponibles y si estan activas o no, eso se ve si estan sincronizados con el dictonary del schedule.
(listo) 2_ Crear un Schedule.
3_ crear un txt de "default" por si envia un mensaje que no se consiga en un comando, por si quieren que yo lo suba a mano
(listo) 4_ Configurar el bot para leer los emojis y convertirlos en ID en formato HTML
5_ el mensaje status tenga formato mas lindo, ejemplo reload el emoji reload,
mensaje [carpeta] tengo emoji buzon
carpetas emoji carpetas
horarios emoji relog
y en guardar caption una linea recta separando lo importante y si puede darse mas formato mejor
6_ que el acomando "Mensaje test" envie el mensaje si no posee mensaje se envie solo texto, y si no tiene texto que envie solo imagen, pero no salte error por la falta de imagen
7_ en lugar de que se guarde "internamente" la variable programacion, carpetas, que se guarde como archivo fisico, por si hay una desconeccion que no haya una re-descarga, obviamente si el dia es nuevo, pisarlo, asi mantenemos una lectura de una sola ves.
8_ un comando que diga "id [chat] y extraiga el id del chat en cuestion que se quiera extraer para agregarlo manualmente a la configuracion.
9_ modificar los logs para que se escriba de manera correcta y el ultimo mensaje sea "Bot escuchando comandos" cuando este todo cargado